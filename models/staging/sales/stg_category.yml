version: 2

models:
  - name: stg_category
    description: Staging model for raw sales data
    columns:
      - name: category_key
        description: A surrogate key for customer dimension
        tests:
          - unique
          - not_null
          
      - name: category
        description: Unique identifier for customer
        tests:
          - unique
          - not_null
      